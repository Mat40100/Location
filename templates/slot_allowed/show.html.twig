{% extends 'base.html.twig' %}

{% block title %}SlotAllowed{% endblock %}

{% block body %}
    <h1>SlotAllowed</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slot_allowed.id }}</td>
            </tr>
            <tr>
                <th>StartTime</th>
                <td>{{ slot_allowed.startTime ? slot_allowed.startTime|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EndTime</th>
                <td>{{ slot_allowed.endTime ? slot_allowed.endTime|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('slot_allowed_index') }}">back to list</a>

    <a href="{{ path('slot_allowed_edit', {'id': slot_allowed.id}) }}">edit</a>

    {{ include('slot_allowed/_delete_form.html.twig') }}
{% endblock %}
