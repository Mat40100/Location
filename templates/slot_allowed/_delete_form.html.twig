<form method="post" action="{{ path('slot_allowed_delete', {'id': slot_allowed.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ slot_allowed.id) }}">
    <button class="btn">Delete</button>
</form>
